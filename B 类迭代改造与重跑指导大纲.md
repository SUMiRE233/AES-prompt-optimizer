以下内容可直接作为实验环境 `C:\Users\SUMiRE\Desktop\分层分档 - ds` 的执行指导。核心要求是：本任务必须完成实际代码接线与离线验证，不能只新增设计文档或测试孤立辅助函数。

# B 类迭代改造与重跑指导大纲

> **2026-09-21 修订（统一 Q 协议）**：B 类已按 `Q = 2.5*mean(Severe)+mean(Soft)`、
> 两级精度协议（低精度单评 -> 首次预警永久双评 + 边界复核 -> 上限强制补评）与
> route-aware 聚合完成重验证，并以实验性口径冻结（`B_EXPLORATORY_PROTOCOL_FROZEN`，final=V6）。
> 本大纲 §3.2 / §3.3 / §9.2 / §十 / §十一 / §十二 / §十三 已就地修订；
> 完整执行与冻结记录见 `B_REFACTOR_REPORT.md` §12.7。

## 一、任务目标

在不引入交叉验证、不改变现有简单留出协议、不给 MAE gate 权限的前提下，完成 B 类流程改造并从 `origin_prompt` 重新运行 B 类迭代。

本阶段只完成：

1. 固定并验证 B 类可修改结构。
2. 防止标题漂移、结构膨胀和无效 meta 内容。
3. 确保 origin、候选版本和 final 均满足三维锚点前置条件。
4. 保留现有 B 类停止与 final 选择规则。
5. 使用固定简单留出集完成实际 B 类重跑。
6. 形成可复核、可冻结的客观检查报告。

本阶段不实施、运行或宣称完成 E 类迭代。E 类路线必须等待新的 B final 真实生成并验收后再讨论。

------

## 二、强制实施要求

### 2.1 特别提醒：禁止“只有大纲，没有实施”

目标模型此前出现过以下问题：

- 新增了设计文档，但没有修改生产代码。
- 实现了辅助校验函数和单元测试，但没有接入 optimizer、pipeline 或候选保存路径。
- 测试验证了孤立函数，却没有验证真实调用链。
- 在报告中把“计划实现”写成“已经完成”。

本任务属于实际改造任务，不是设计任务。

满足以下全部条件才可声称某项功能已经完成：

1. 生产入口已经调用对应逻辑。
2. 非法输入能够在真实路径中被拒绝。
3. 有覆盖实际接线的测试，而不只是辅助函数测试。
4. 已运行测试并记录命令、数量和结果。
5. 报告能够指出具体文件、函数和行为证据。
6. 不得以新增 Markdown 设计稿代替代码实现。

如果任何要求只完成了设计或辅助函数，应在报告中标为“未实施”或“部分实施”，不得标为“通过”。

------

## 三、保留不变的实验协议

### 3.1 数据协议

继续使用既有简单留出流程：

- 36 篇训练集：用于 B 类优化和训练侧 badcase 统计。
- 12 篇留出集：只用于 gate 触发后的候选版本比较。
- 不引入 CV。
- 不重新随机划分。
- 不使用实验目录中新增的 `cv_folds.json`、`cv_runner.py` 或 `cv_runs` 作为本轮执行入口。

重跑前必须核对并记录：

- 36/12 的实际全局 index。
- train/test 是否互斥并完整覆盖 48 篇。
- 当前评分产物是否与对应作文文件拥有完全相同的 index 集合。
- 数据文件 hash。

旧评分结果如果不对应当前划分，必须归档，不得复用。

### 3.2 B 类停止规则（2026-09-21 统一 Q 协议版）

```
Q = 2.5 * mean(Severe) + mean(Soft)   # 均值为该版本在当前精度等级下全部固定次数评测
```

- 自动迭代安全上限为 V6。

- **判定**：`Q_n >= Q_{n-1}`（含平台期）即触发；仅严格下降才算通过。

- **低精度阶段**（每版单评）：首次触发视为**低精度预警**，不立即停止；
  **永久**升级为双评并补评 `x-1` 与 `x`（不回溯更早版本），以双评均值复核：
  仍触发 -> 停止（候选 `x-1`、`x`）；被推翻 -> 接受 `x` 继续（此后固定双评、不可回退）。

- **高精度阶段**：每版固定双评，`Q_n >= Q_{n-1}` 即停止（有效 gate，候选 `n-1`、`n`）。

- **V6 上限**：从未预警时在 V6 强制提升精度，**只**补评 V5、V6；上限处的高精度 gate
  记为 `high_precision_gate_at_cap`。

- 权重 w=2.5 为位于 (2,3) 内的业务风险权重（防早停 + 防 `+1S/-2F` 套利），
  不是对结果拟合的精确兑换率；不得自行修改 w、精度分级或停止条件。

### 3.3 final 选择规则（2026-09-21 统一 Q 协议版）

- **候选**：停止点（gate 或上限）的前后两版 `n-1`、`n`（本轨迹为 V5、V6）。

- **validation**：两候选使用同一留出样本（12 篇），**各自固定两次独立评测**
  （不依第一次结果决定第二次），取 Severe/Soft 算术平均。

- **排序**：
  1. 期望 `Q` 更低（`Q = 2.5*mean(Severe) + mean(Soft)`）；
  2. `Q` 相同时，均值 Severe 更少；
  3. 仍相同时，选择更早版本。

- MAE 只记录并在回升时告警，不参与排序。

### 3.4 MAE 政策

MAE：

- 不参与停止 gate。
- 不参与 final 排序。
- 必须逐版本记录。
- 若候选相对前一版本或稳定版本回升，输出明确 warning。
- 报告不得隐去 MAE 回升。

不得以“MAE 更优”为理由覆盖既定 Q 选版结果，也不得以“MAE 不参与 gate”为理由不报告它。

------

## 四、origin prompt 结构改造

### 4.1 必备结构

`origin_prompt_meta.md` 和部署后的 `origin_prompt.md` 必须在 `## 注意事项` 中各有且仅有一个：

- `**内容分特殊情形**`
- `**表达分特殊情形**`
- `**结构分特殊情形**`

表达维原有的“语言分评价范围”内容应迁移到 `**表达分特殊情形**` 下，不应丢失原规则语义。

### 4.2 Meta 与部署一致性

删除或隔离不会进入实际评分请求的 meta 冗余区段。必须验证：

- origin meta 经预处理后与 origin runtime prompt 的有效评分内容一致；
- 不得保留会让 optimizer 修改、但部署时又被删除的伪优化空间；
- 不得删除真实评分所需内容。

------

## 五、B 类修改权限

### 5.1 冻结内容

每轮 B 优化都不得修改：

- 顶层 `##` 标题集合、名称及顺序；
- 所有已有加粗分点的标题及顺序；
- 内容/表达/结构三个特殊情形锚点；
- prompt 的基础输出协议。

禁止：

- 删除、改名、合并或重排已有加粗分点；
- 新增顶层 `##` 章节；
- 使用 `###`；
- 将某维评分规则迁移到另一个维度；
- 用新增标题重复表达已有锚点；
- 将完整 prompt 扩张为规则百科。

### 5.2 默认允许的修改

优先允许：

- 修改已有加粗分点下的正文；
- 新增、修改或删除已有分点下的项目符号；
- 将新规则写入对应维度的“特殊情形”分点；
- 压缩重复或无效表述。

### 5.3 新增加粗分点

仅当规则无法合理放入现有分点时，才允许增加操作型加粗分点。

新增分点必须满足：

- 每轮最多 2 个；
- 只能表达评分流程或输出前自查；
- 标题不得包含内容、表达、语言、结构等维度名称；
- 不得是新的评分锚点、上下限、区间、校准或扣分限制；
- 不得重复已有标题；
- 不得改变三个特殊情形分点的唯一性。

------

## 六、结构约束必须接入真实路径

不得只在 optimizer system prompt 中声明约束。

真实流程必须为：

```
读取当前 prompt
→ 验证当前 prompt 输入契约
→ 调用 optimizer
→ 提取候选 prompt
→ 对 before/after 执行修改权限校验
→ 验证候选输入契约
→ 验证预算
→ 合格后原子写入目标文件
→ 不合格则保留拒绝原因，不覆盖目标文件
```

必须覆盖：

- 标题集合和顺序；
- 重复标题；
- 三维特殊情形唯一性；
- 每轮新增分点数；
- 总分点数；
- prompt 总长度；
- 单轮净增长及实际新增内容量；
- 顶层章节；
- `###`；
- 新增维度型标题。

只调用校验器测试、但 optimizer 仍直接 `save_prompt()`，视为未完成。

------

## 七、候选写入和失败处理

B 候选必须事务化：

1. 优化器输出先写入临时候选。
2. 候选通过全部校验后才能替换目标版本文件。
3. 校验失败时：
   - 保存拒绝原因；
   - 不生成可被下一轮消费的正式版本；
   - 不修改当前稳定版本；
   - 可按限定次数重新请求。
4. 重试耗尽后停止当前 B 迭代，并报告阻塞原因。
5. 不得将非法候选预处理、评分或计入版本历史。

------

## 八、结构与膨胀预算

预算值应在代码中有单一来源，optimizer 提示词引用同一组常量，避免提示词和校验器阈值漂移。

至少记录：

- 每版字符数；
- 相对前版增加字符数；
- 顶层标题数；
- 加粗分点数；
- 新增加粗分点；
- 被删除或改写的项目符号。

候选超预算时必须被拒绝，而不是只输出 warning。

------

## 九、运行产物与可追溯性

### 9.1 重跑前归档

将旧 B 产物统一移动到带时间戳或 run ID 的归档目录，包括：

- `optimized_prompt*_meta.md`
- `optimized_prompt*.md`
- `train_scoring_results*.json`
- `test_scoring_results*.json`
- `aes_badcases*.json`
- `iteration_history.json`
- `b_gate_report.json`
- 旧 final 文件

不得删除无法恢复的历史证据。

### 9.2 新运行 manifest

每次运行至少记录：

- run ID；
- origin 数据 hash；
- train/test index 及 hash；
- origin meta/runtime prompt hash；
- scoring model；
- optimizer model；
- API URL 标识，但不得记录 API key；
- severe/soft 阈值；
- 统一 Q 协议声明（w、精度分级、聚合与 validation 规则）；
- 停止策略；
- 最大版本；
- 结构预算；
- 每版输入和输出文件 hash；
- 每版校验结果。

模型、数据、prompt 或阈值不一致时，禁止自动复用旧产物；策略声明字段
（`stop_policy` / `protocol`）可在登记 `policy_migrations` 后受控迁移。

------

## 十、B 类实际重跑（2026-09-21 统一 Q 协议版）

通过全部离线检查后，从新的 `origin_prompt` 开始：

1. 评分 V0；生成 V0 badcase。
2. 低精度阶段：逐版单评；严格下降则继续。
3. 首次 `Q_n >= Q_{n-1}`：永久升级为双评，补评 `x-1`、`x` 并复核（确认 -> 停止；推翻 -> 继续）。
4. 高精度阶段：逐版固定双评，gate 触发即停止。
5. 到达 V6 且从未预警：强制补评 V5、V6（标记 `high_precision_gate_at_cap` 或 `cap_reached`）。
6. 对候选 `(n-1, n)` 各做两次 validation 评测，按 Q -> Severe -> 更早版本选出 final。
7. 保存 final meta、runtime prompt、badcase、训练评分、validation 报告与证据文件
   （`final_evidence.json`、`b_rebuilt_route.json`）。
8. 对 final 再执行一次完整结构契约检查。
9. 到此停止，不进入 E。

如果某版本没有 B severe/soft badcase，不得发生除零异常。应明确记录“无可供优化的 B badcase”，
并按预先定义的终止行为处理。精度切换点前的历史 rerun 只有在与协议路线位置一致时才可复用，
其余 rerun 仅作探索性留档（`excluded`），不得进入均值。

------

## 十一、必须增加的测试

至少包括：

### 结构接线测试

- optimizer 输出改名时不会写入正式候选；
- 删除标题时拒绝；
- 重排标题时拒绝；
- 重复已有标题时拒绝；
- 新增超过两个加粗分点时拒绝；
- 新增维度型标题时拒绝；
- 非法候选不会被下一轮读取；
- origin 和最终候选均满足三维特殊情形唯一性。

### Pipeline 测试（统一 Q 协议）

- V0 到 V1 使用经过校验的候选；校验失败后当前稳定 prompt 不变；
- 空 B-badcase 不除零；
- `Q` 严格下降继续；平台（ΔQ>=0）触发；
- 低精度预警 -> 双评复核：确认停止 / 推翻继续（升级不可回退）；
- 高精度 gate 停止；上限处记为 `high_precision_gate_at_cap`；
- V6 上限强制补评只触及 V5、V6；
- route-aware 聚合：路线外 rerun 不计入均值（`excluded`）；
- final 排序严格遵守 Q、均值 Severe、较早版本；
- 每候选固定两次 validation 评测（不依第一次结果决定第二次）；
- MAE 回升只 warning，不改变选择。

### 数据与产物测试

- train/test index 完全互斥并覆盖 origin；
- 每个 scoring 文件的 index 必须和对应 essays 文件一致；
- 旧 run manifest 不匹配时不得复用；
- API key 不进入日志、manifest 或提交文件。

------

## 十二、客观检查报告要求

最终必须提交一份客观检查报告，不得只给结论性描述。

报告至少包含以下表格。

### 12.1 要求落实表

| 要求             | 状态              | 生产代码位置 | 测试位置 | 实际证据 |
| ---------------- | ----------------- | ------------ | -------- | -------- |
| 三维锚点唯一     | PASS/FAIL/PARTIAL | 文件及函数   | 测试名   | 校验输出 |
| B 标题冻结       |                   |              |          |          |
| 候选写入前校验   |                   |              |          |          |
| 非法候选不落盘   |                   |              |          |          |
| 简单留出保持不变 |                   |              |          |          |
| MAE 不参与 gate  |                   |              |          |          |
| 运行 manifest    |                   |              |          |          |

状态定义：

- `PASS`：生产路径已接入，并有实际测试证据。
- `PARTIAL`：存在辅助实现，但未完全接入或覆盖。
- `FAIL`：未实现、行为不符或测试失败。

### 12.2 逐版本结果

| 版本 | Severe(均值) | Soft(均值) | Q | MAE  | MAE warning | 字符数 | 加粗分点数 | 结构契约 |
| ---- | ------ | ---- | ----- | ---- | ----------- | ------ | ---------- | -------- |
| V0   |        |      |       |      |             |        |            |          |
| V1   |        |      |       |      |             |        |            |          |
| …    |        |      |       |      |             |        |            |          |

### 12.3 Gate 与 final

必须报告：

- 停止触发版本及原因（gate / `high_precision_gate_at_cap` / `cap_reached`）；
- 被比较版本（候选 `n-1`、`n` 或 V5、V6）；
- 每个候选的 validation 双评明细、均值 Severe/Soft 与 Q；
- MAE 及 warning；
- 排序过程；
- final 版本；
- final prompt hash；
- final 结构检查结果；
- 协议状态与审计链（`b_protocol_state.json` / `b_rerun_log.json` / `b_rebuilt_route.json`）。

### 12.4 未完成与风险

必须如实列出：

- 尚未接入的辅助模块；
- 只完成设计、未完成实现的内容；
- 未覆盖的测试分支；
- 简单 12 篇留出带来的代表性限制；
- 模型非确定性；
- 任何人工操作步骤。

禁止使用“总体完成”“基本可用”等模糊表述掩盖部分实施。

------

## 十三、冻结判定

只有以下条件全部满足，才可将 B 类标记为“可冻结”：

- 结构约束已接入生产路径；
- origin、所有正式候选和 final 均通过契约；
- 旧产物已归档，新旧 run 不混用；
- 固定 36/12 划分得到确认；
- 离线测试全部通过；
- 实际 B 重跑完整结束；
- gate 和 final 选择符合预定规则；
- MAE 只记录和告警；
- final 结构稳定，可供后续 E 使用；
- 客观检查报告无未披露的 `PARTIAL` 或 `FAIL`。

状态标记（2026-09-21 更新）：

```
B_RESULT_AVAILABLE_BUT_NOT_FROZEN   # final 已选出但结构/身份/接线不完整（历史状态）
B_EXPLORATORY_PROTOCOL_FROZEN       # 实验性口径冻结：协议 + 候选 + 证据就绪（当前，2026-09-21）
B_FROZEN_READY_FOR_E_DESIGN         # 确认性口径（需独立 test；当前为 NONE，未达成）
```

当前实际状态：**`B_EXPLORATORY_PROTOCOL_FROZEN`**（final=V6；`INDEPENDENT_TEST=NONE`；
E 状态 `DEFERRED`）。在进入 E 类机制修改、规则生成或联网测试前，需先完成 E 路线设计
（可消费 `final_train_scoring_results_mean.json` 均值基准）。
